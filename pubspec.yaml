name: cloud_build_engine_app
description: Cloud APK Build Engine - Flutter mobile app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  file_picker: ^8.1.6
  path_provider: ^2.1.5
  intl: ^0.19.0
  archive: ^4.0.2
  share_plus: ^10.1.4
  open_filex: ^4.5.0
  uuid: ^4.5.1
  percent_indicator: ^4.2.3
  google_fonts: ^6.2.1
  shimmer: ^3.0.0
  flutter_animate: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
